import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'miAppMovil-2063584',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
